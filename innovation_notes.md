# 创新点备忘（项目落地后可展开）

## 方向名称
**角度置信度驱动的稳健去系数与加权联合估计（for MIMO-OFDM 三维联合估计）**

---

## 背景问题
在三维联合估计中，若先做角度粗估再去除信号相关项（如 \(a^H(u,v)x\)），会出现：
1. 角度失配导致后续距离/速度估计偏差（误差传播）；
2. 当 \(|d|=|a^H(u,v)x|\) 过小时，直接相除会放大噪声，速度维最敏感。

---

## 核心创新思想（可作为方法改进点）
1. **引入缩放因子思想（\(\alpha\)）维持能量一致性**：
   - 在去系数过程中保持角度bin间功率关系，避免幅度结构被破坏。
2. **正则化去系数替代直接除法**：
   - 采用 \(\tilde y = Y\cdot \overline{d}/(|d|^2+\epsilon)\)，降低近零除法风险。
3. **低置信度样本降权（门限升级为软权重）**：
   - 基于 \(|d|\) 构造 \(w_d\)；
   - 基于角度谱峰尖锐度构造 \(w_{ang}\)；
   - 联合权重 \(w = w_d\cdot w_{ang}\)，用于R/v估计。
4. **多候选角度融合与轻量迭代细化**（可选增强）：
   - 避免单一角度bin硬判决导致误差放大。

---

## 可落地实现要点（工程版）
- 在 `local_ESPRIT_estimator_3d` 中新增：
  - `params.local_esprit.eps_div`（正则化项）
  - `params.local_esprit.tau_d`（|d|置信门限）
  - `params.local_esprit.tau_sharp`（角度谱峰尖锐度门限）
  - `params.local_esprit.use_weighted_rv`（是否启用加权R/v）
- 在 `info` 输出中记录：
  - `d_abs_stats`、`angle_sharpness`、`sample_weights`、`weighted_fit_status`

---

## 预期收益
1. 降低低SNR条件下速度估计抖动与RMSE尾部风险；
2. 缓解角度误差向距离/速度维传播；
3. 对近邻目标场景更稳定（减少配对错位）。

---

## 验证建议（后续可做）
- 对比组：
  1. 原始直接去系数；
  2. 正则化去系数；
  3. 正则化 + 联合降权（完整方案）。
- 指标：
  - `rmse_R`, `rmse_v`, p95误差、漏检率、算法耗时。
- 场景：
  - SNR = 0/5/10 dB；
  - 近邻目标（距离间隔 <= 0.8 m）；
  - 500~600 m范围。

---

## 备注
该点属于“稳健联合估计”的**方法创新/工程创新**，适合作为项目落地后的论文或专利储备方向。